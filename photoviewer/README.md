# Photo Viewer

A tiny standalone photo viewer. Open `index.html` in Chrome or Edge, click
"Choose Folder", and pick a folder on your hard drive. It loads all images
in that folder (subfolders are ignored) and lets you navigate with the
left/right arrow keys.

No build step, no dependencies — just a single HTML file.
